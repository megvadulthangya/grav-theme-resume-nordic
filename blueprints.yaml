name: Nord Resume
slug: resume-nordic
type: theme
version: 1.0.0
description: "A modern resume theme."
icon: user-secret
author:
  name: Gyöngyösi Gábor
  email: gyongyosigabor@gshoots.hu
homepage: https://github.com/megvadulthangya/grav-theme-resume-nordic
demo: https://nordic-skeleton.gshoots.hu
license: MIT

dependencies:
  - { name: grav, version: '>=1.7.0' }
  - { name: error }
  - { name: problems }

form:
  validation: loose
  fields:
    section_avatar:
        type: section
        title: Avatar Settings
        underline: true
    
    custom_avatar_image:
      type: file
      label: Custom Local Avatar
      help: "Upload your own profile picture here. Overrides Gravatar."
      destination: 'theme://assets'
      multiple: false
      accept:
        - image/*
    
    gravatar_email:
        type: email
        label: Gravatar Email
        help: "Used for Gravatar if no local image is uploaded. Leave empty to use site default."
        placeholder: "example@gmail.com"

    gravatar_enabled:
        type: toggle
        label: Enable Avatar Area
        highlight: 1
        default: 1
        options:
          1: PLUGIN_ADMIN.ENABLED
          0: PLUGIN_ADMIN.DISABLED
        validate:
          type: bool

    gravatar_size:
        type: range
        label: Avatar Size (px)
        default: 200
        min: 50
        max: 500
        step: 10
        validate:
          min: 50
          max: 500
          type: number

    section_footer:
        type: section
        title: Footer Settings
        underline: true

    footer_text:
        type: text
        label: Custom Footer Name
        help: "Date (© 2025) is automatic. Enter name/company only. Defaults to Site Author."
        placeholder: "e.g. My Company Ltd."

    hide_credits:
        type: toggle
        label: Hide Credits
        highlight: 0
        default: 0
        options:
          1: PLUGIN_ADMIN.YES
          0: PLUGIN_ADMIN.NO
        validate:
          type: bool
